server:
  servlet:
    port: 8080
    contextPath: /api

spring:

  application:
    name: technical-api

  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ImageHosterAPIEndPoints
    username: postgres
    password: superuser

  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false
    database-platform: org.hibernate.dialect.PostgreSQL82Dialect